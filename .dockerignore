# Archivos a ignorar al construir la imagen Docker

# Base de datos SQLite (se genera en runtime, se persiste en volumen)
data/transitabilidad_history.db

# GeoJSON de departamentos (se descarga y cachea automáticamente)
data/departments_geojson.json

# NOTA: data/latest_abc_data.json y data/manual_abc_data.json
# SÍ se incluyen en el build — se copian como seed data en /app/data-seed/
# para que el servidor tenga datos desde el primer arranque.

# Python compilado
__pycache__/
*.py[cod]
*.pyo

# Entornos virtuales
venv/
env/
.venv

# Archivos del sistema
.DS_Store
Thumbs.db
*.swp

# Git
.git/
.gitignore

# Archivos solo para desarrollo local
*.bat
iniciar_linux.sh
permitir_firewall_windows.bat
mantener_mapa.py
logs/
