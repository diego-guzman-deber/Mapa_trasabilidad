# Python
__pycache__/
*.py[cod]
*.pyo
.Python

# Entornos virtuales
venv/
env/
.env
.venv

# Sistema
.DS_Store
Thumbs.db
*.swp

# Logs (generados en runtime, no en repo)
logs/

# La DB SQLite se genera en runtime — no subir al repo
data/transitabilidad_history.db

# NOTA: los archivos JSON de data/ SÍ se incluyen en el repo:
#   latest_abc_data.json, manual_abc_data.json, departments_geojson.json
